// This file was auto-generated by the veyron vdl tool.
// Source(s):  service.vdl
package com.veyron.runtimes.google.ipc.benchmarks.vdlgen;

import com.google.common.reflect.TypeToken;
import com.veyron2.OptionDefs;
import com.veyron2.Options;
import com.veyron2.RuntimeFactory;
import com.veyron2.ipc.Context;
import com.veyron2.ipc.VeyronException;
import com.veyron2.vdl.ClientStream;
import java.util.ArrayList;

public class Client { 
	/* Bind methods for interfaces in file: service.vdl. */
	public static Benchmark bindBenchmark(String name) throws VeyronException {
		return bindBenchmark(name, null);
	}
	public static Benchmark bindBenchmark(String name, Options veyronOpts) throws VeyronException {
		com.veyron2.ipc.Client client = null;
		if (veyronOpts != null && veyronOpts.get(OptionDefs.CLIENT) != null) {
			client = veyronOpts.get(OptionDefs.CLIENT, com.veyron2.ipc.Client.class);
		} else if (veyronOpts != null && veyronOpts.get(OptionDefs.RUNTIME) != null) {
			client = veyronOpts.get(OptionDefs.RUNTIME, com.veyron2.Runtime.class).getClient();
		} else {
			client = RuntimeFactory.getRuntime().getClient();
		}
		return new BenchmarkStub(client, name);
	}

	/* Client stubs for interfaces in file: service.vdl. */
	private static class BenchmarkStub implements Benchmark {
		private static final String vdlIfacePathOpt = "com.veyron.runtimes.google.ipc.benchmarks.vdlgen.Benchmark";
		private final com.veyron2.ipc.Client client;
		private final String name;

		BenchmarkStub(com.veyron2.ipc.Client client, String name) {
			this.client = client;
			this.name = name;
		}
		// Methods from interface Benchmark.
		@Override
		public ArrayList<Byte> echo(Context context, ArrayList<Byte> Payload) throws VeyronException {
			return echo(context, Payload, null);
		}
		@Override
		public ArrayList<Byte> echo(Context context, ArrayList<Byte> Payload, Options veyronOpts) throws VeyronException {
			// Prepare input arguments.
			final Object[] inArgs = new Object[]{ Payload };

			// Add VDL path option.
			// NOTE(spetrovic): this option is temporary and will be removed soon after we switch
			// Java to encoding/decoding from vom.Value objects.
			if (veyronOpts == null) veyronOpts = new Options();
			if (!veyronOpts.has(OptionDefs.VDL_INTERFACE_PATH)) {
				veyronOpts.set(OptionDefs.VDL_INTERFACE_PATH, BenchmarkStub.vdlIfacePathOpt);
			}

			// Start the call.
			final com.veyron2.ipc.Client.Call call = this.client.startCall(context, this.name, "Echo", inArgs, veyronOpts);

			// Prepare output argument and finish the call.
			final TypeToken<?>[] resultTypes = new TypeToken<?>[]{ new TypeToken<ArrayList<Byte>>() {} };
			return (ArrayList<Byte>)call.finish(resultTypes)[0];

		}
		@Override
		public ClientStream<ArrayList<Byte>,ArrayList<Byte>,Void> echoStream(Context context) throws VeyronException {
			return echoStream(context, null);
		}
		@Override
		public ClientStream<ArrayList<Byte>,ArrayList<Byte>,Void> echoStream(Context context, Options veyronOpts) throws VeyronException {
			// Prepare input arguments.
			final Object[] inArgs = new Object[]{  };

			// Add VDL path option.
			// NOTE(spetrovic): this option is temporary and will be removed soon after we switch
			// Java to encoding/decoding from vom.Value objects.
			if (veyronOpts == null) veyronOpts = new Options();
			if (!veyronOpts.has(OptionDefs.VDL_INTERFACE_PATH)) {
				veyronOpts.set(OptionDefs.VDL_INTERFACE_PATH, BenchmarkStub.vdlIfacePathOpt);
			}

			// Start the call.
			final com.veyron2.ipc.Client.Call call = this.client.startCall(context, this.name, "EchoStream", inArgs, veyronOpts);

			return new ClientStream<ArrayList<Byte>, ArrayList<Byte>, Void>() {
				@Override
				public void send(ArrayList<Byte> item) throws VeyronException {
					call.send(item);
				}
				@Override
				public ArrayList<Byte> recv() throws java.io.EOFException, VeyronException {
					final TypeToken<?> type = new TypeToken<ArrayList<Byte>>() {};
					final Object result = call.recv(type);
					try {
						return (ArrayList<Byte>)result;
					} catch (ClassCastException e) {
						throw new VeyronException("Unexpected result type: " + result.getClass().getCanonicalName());
					}
				}
				@Override
				public Void finish() throws VeyronException {
					// Prepare output argument and finish the call.
					final TypeToken<?>[] resultTypes = new TypeToken<?>[]{  };
					call.finish(resultTypes);
					return null;

				}
			};
		}
	}
}
