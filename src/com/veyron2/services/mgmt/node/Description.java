
// This file was auto-generated by the veyron vdl tool.
// Source: node.vdl
package com.veyron2.services.mgmt.node;

import java.util.HashSet;

/**
 * Description enumerates the profiles that a Node supports.
**/
public final class Description { 
	// Profiles is a set of names of supported profiles.	Each name can
// either be an object name that resolves to a Profile, or can be the
// profile's label, e.g.:
//   "profiles/google/cluster/diskfull"
//   "linux-media"
//
// Profiles for nodes can be provided by hand, but they can also be
// automatically derived by examining the node.
	private HashSet<String> profiles;

	public Description(HashSet<String> profiles) { 
		this.profiles = profiles;
	}
	public HashSet<String> getProfiles() { return this.profiles; }

	public void setProfiles(HashSet<String> profiles) { this.profiles = profiles; }

	@Override
	public boolean equals(java.lang.Object obj) {
		if (this == obj) return true;
		if (obj == null) return false;
		if (this.getClass() != obj.getClass()) return false;
		final Description other = (Description)obj;
		if (!(this.profiles.equals(other.profiles))) return false;
		return true;
	}
	@Override
	public int hashCode() {
		int result = 1;
		final int prime = 31;
		result = prime * result + (profiles == null ? 0 : profiles.hashCode());
		return result;
	}
}
