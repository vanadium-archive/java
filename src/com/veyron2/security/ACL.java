
// This file was auto-generated by the veyron vdl tool.
// Source: types.vdl
package com.veyron2.security;

import java.util.HashMap;

/**
 * ACL pairs principal sets with label sets. This should be treated as an
 * unordered set, not as a dictionary.  A client with PublicID <pid> is
 * allowed access using <label> if there exists a pair (principals, labels)
 * in the ACL such that <pid>.Match(principals) and labels.HasLabel(label).
**/
public final class ACL { 
		private HashMap<PrincipalPattern, LabelSet> value;

	public ACL(HashMap<PrincipalPattern, LabelSet> value) { 
		this.value = value;
	}
	public HashMap<PrincipalPattern, LabelSet> getValue() { return this.value; }

	public void setValue(HashMap<PrincipalPattern, LabelSet> value) { this.value = value; }

	@Override
	public boolean equals(java.lang.Object obj) {
		if (!(obj instanceof ACL)) return false;
		final ACL other = (ACL)obj;
		if (!(this.value.equals(other.value))) return false;
		return true;
	}
}
