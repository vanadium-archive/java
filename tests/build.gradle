apply plugin: 'java'

dependencies {
    compile group: 'junit', name: 'junit', version: '4.12'
    compile group: 'com.google.truth', name: 'truth', version: '0.25'
    compile project(':lib')
}

repositories {
    mavenCentral()
}

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

