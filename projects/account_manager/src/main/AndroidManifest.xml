<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.v.android.apps.account_manager"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="20" />

    <!-- Needed for Security -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Needed for RPC -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Needed for Account Management -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/launchericon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo" >
        <service
            android:name=".AuthenticatorService"
            android:exported="true"
            android:label="@string/authenticator_service_name" >
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

        <activity
            android:name=".AccountActivity"
            android:label="@string/title_activity_account" >
        </activity>

        <!--
             android:windowSoftInputMode="adjustPan" below is necessary to prevent
             the keyboard input from moving layout views around.
        -->
        <activity
            android:name=".BlessingActivity"
            android:label="@string/title_activity_blessing"
            android:windowSoftInputMode="adjustPan" >
            <intent-filter>
                <action android:name="io.v.android.apps.account_manager.BLESS" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" >
            <intent-filter>
                <action android:name="io.v.android.apps.account_manager.SETTINGS" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BlessedPackagesDisplayActivity"
            android:label="@string/title_activity_blessed_packages_display" >
            <intent-filter>
                <action android:name="io.v.android.apps.account_manager.BLESSED_PACKAGES_DISPLAY" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BlessingEventsDisplayActivity"
            android:label="@string/title_activity_blessing_events_display" >
            <intent-filter>
                <action android:name="io.v.android.apps.account_manager.BLESSING_EVENTS_DISPLAY" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AccountChooserActivity"
            android:label="@string/title_activity_account_chooser"
            android:theme="@android:style/Theme.Holo.Dialog">
        </activity>

    </application>

</manifest>
